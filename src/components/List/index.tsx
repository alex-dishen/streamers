import { ListProps } from 'components/List/types';
import { ListWrapper } from 'components/List/styles';

const List = ({}: ListProps) => {
  return <ListWrapper></ListWrapper>;
};

export default List;
