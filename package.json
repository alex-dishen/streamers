{
  "name": "game-harbor",
  "version": "1.0.0",
  "scripts": {
    "client:i": "cd client && yarn",
    "server:i": "cd server && yarn",
    "client": "cd client && yarn start",
    "server": "cd server && yarn dev"
  },
  "devDependencies": {
    "react-cssfx-loading": "^2.1.0"
  }
}
