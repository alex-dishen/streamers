export type BreakPointsT = {
  sm: string;
  md: string;
  lg: string;
};

export type FontSizesT = {
  '2xs': string;
  xs: string;
  sm: string;
  md: string;
  lg: string;
  xl: string;
};

export type ColorsT = {
  black: {
    900: string;
  };
  gray: {
    400: string;
  };
  blue: {
    600: string;
  };
  red: {
    200: string;
  };
};
